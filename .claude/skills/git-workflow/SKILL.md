---
name: git-workflow
description: >-
  브랜치를 만들고 GitHub PR을 열거나 고친다. 이렇게 부를 때 쓴다: "PR 작성하자", "PR 이어서 작업하자", "open the PR".
---

이 저장소는 `git-workflow` 스킬의 배포 원본을 직접 보유하고 있다. 따라서 사본을 두지
않고 원본 템플릿을 그대로 참조한다. 내용을 고칠 때에는 아래 원본 파일만 수정한다.

이 저장소는 GitHub를 사용하므로 GitLab 템플릿이 아니라 GitHub 템플릿을 참조한다.

@plugins/project-skills-bootstrap/skills/bootstrap-git-workflow/git-workflow-github.md
